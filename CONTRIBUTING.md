# in Mediana

! README](README.md) (north star project) and this ## North star project

**level ** and third-party (not-Microsoft) in any PR, allocations on or dependency in will if not metric — [in CI](.github/workflows/ci.yml) not must ## NET SDK 10.0.302+ (`global.json` for tests: Docker (Testcontainers) — without Docker for NativeAOT-builds VS C++ workload (in CI is)

## PR (all CI)

```bash
dotnet build -c Release # 0 warnings as errors)
dotnet test tests/Mediana.UnitTests # + dotnet test tests/Mediana.UnitTests.Ns21 # those tests ns2.1-dotnet test tests/Mediana.GeneratorTests # source generator
dotnet test tests/Mediana.ContractTests.Ns21 # API TFM
dotnet test tests/Mediana.InteropTests # MediatR-+ telemetry

# powershell -File scripts/check-coverage.ps1 # union branch coverage >= 95%
dotnet tool restore
dotnet tool run dotnet-stryker # mutation score >= 90%
dotnet run --project benchmarks/Mediana.Benchmarks -c Release -- alloc-check # 0 B/# benchmarks (if dotnet run --project benchmarks/Mediana.Benchmarks -c Release -- load-check all
dotnet run --project benchmarks/Mediana.Benchmarks -c Release -- ram-check all
```

results in `benchmarks/RESULTS.md` (+ methodology).

## C# latest, `Nullable` + `TreatWarningsAsErrors` — without canon-generic `RequestCallSiteCompositor` and tests `AllocationBisectTests`) — invoke generic-from canon-~24–32 public API without `required`-ns2.1), without on `[RequiresDynamicCode]`/`[RequiresUnreferencedCode]`
- `// Stryker disable` tests: behavioral-tests for for branches to 95% union

## commits and PR

- `type(scope): message` — types `feat|fix|perf|refactor|test|docs|chore|bench`; breaking changes — `!` and BREAKING CHANGE in PR — tests PR-github/PULL_REQUEST_TEMPLATE.md](.github/PULL_REQUEST_TEMPLATE.md)
- `CHANGELOG.md` — for ## if output `alloc-check`/`load-check`/`ram-check` + `.NET`-and without not in [benchmarks/RESULTS.md](benchmarks/RESULTS.md).
