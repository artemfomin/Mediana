# in > when will capability. on to ## Q1. tests (Testcontainers) — is Docker on ?
**** tests RabbitMQ/Kafka/SQL/Mongo Testcontainers.
**to ** and unit/generator/interop-tests (in-memory/in-process); tests and `[Trait("Category","RequiresDocker")]` on Docker-**** on ## Q2. CI (GitHub Actions?)
**** CI-coverage/mutation/benchmark-diff/dependency-audit).
**** `.github/workflows/ci.yml` + local `scripts/verify.ps1` ## Q3. for outbox-in v1
**** EF Core (net10-only), Dapper (Postgres/SQL Server MongoDB.
**** Dapper-provider Postgres + SQL Server; on ## Q4. public namespace for `Mediana` or `Mediana.Messaging`?
**** `Mediana.Messaging` (envelope), `Mediana` (mediator), `Mediana.Transports.*` (## Q5. mutation Stryker-**** Stryker by Abstractions+Mediana+Transport.Abstractions+Outbox) score ≥90%; transports/in integration-## Q9. mutation ~30 killer-tests (behaviors, + Stryker-on fallback-fast/slow results — CallSiteBranchTests; in only on this Stryker for equivalent mutants.

## Q10. branch coverage ≥95% by UNION Mediana 95.1%, Abstractions 100%, Transport.Abstractions 95.2%, Outbox 100%.
scripts/check-coverage.ps1 (union, in CI for test-branches — per-instantiation generics: value-not ref-and ## Q8. MediatR-bridge MediatR IPipelineBehavior in pipeline Mediana
MediatRBridge (commands/scan, DI). MediatR-behaviors → Mediana behaviors not how roadmap (v1.x). if ## Q7 (in RabbitMQ.Client 7.2.2 netstandard2.0-D13 Mediana.RabbitMQ unified client 7.2.2 on TFM (6.x-not on if x-## Q6. versions NuGet-by on 2026-09-01.
if others MassTransit 