{
  "$schema": "https://raw.githubusercontent.com/InfoSumSchema/jsonnet/optionals/schemas/stryker-config.schema.json",
  "stryker-config": {
    "solution": null,
    "project": "Mediana.csproj",
    "target-framework": "net10.0",
    "reporters": ["progress", "html"],
    "mutation-level": "Standard",
    "thresholds": { "high": 95, "low": 90, "break": 90 },
    "mutate": [
      "!**/*Exception*.cs",
      "**/*.cs"
    ]
  }
}
